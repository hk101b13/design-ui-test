import * as React from "react";
import { isBrowserClient } from "../utils/commonUtil.js";
function useLayoutEffect(effect, deps) {
  if (isBrowserClient) {
    React.useLayoutEffect(effect, deps);
  } else {
    React.useEffect(effect, deps);
  }
}
export {
  useLayoutEffect as default
};
